import React from 'react';

export type Order = {
    id: number;
    orderDate: string;
    status: string;
    customerId: number;

}

const Order = () => {
    return (
        <div>

        </div>
    );
};

export default Order;